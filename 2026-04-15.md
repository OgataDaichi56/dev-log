# 2026-04-15 学習メモ

## 学んだトピック
- Reactコンポーネント・Props・useState でトグル機能実装
- State Lifting（状態を親コンポーネントに引き上げる）
- スキルデータを skills.json に分離（データとUIの分離）
- useEffect で localStorage に完了状態を永続化

## 詰まったこと
- Next.js では localStorage は useEffect 内でのみ使う（サーバー側では使えないため 500 エラーが出た）
- `@/data/skills.json` のパスエラー → ファイルが `src/app/data/` にあったため `./data/skills.json` に修正
- Windows の echo コマンドで作ったファイルが UTF-16 になる問題 → Bash の printf を使って解決

## 成果物
- skill-tree アプリに完了状態のトグル・カウンター・localStorage 永続化を実装

## 次にやること
- Next.js のファイルベースルーティング（ページ遷移）
- Phase 2：TypeScript 入門
