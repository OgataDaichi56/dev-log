# 2026-05-02 学習メモ

## 学んだトピック
- dynamic routing の復習（/skills/[id]）
- `useParams` でクライアントコンポーネントからURLのidを取得
- 詳細ページで localStorage からスキルの完了状態を読み込む
- `useState` の型定義（`| null` の使い方）
- `"use client"` と `async` は共存できない理由（サーバー/クライアントの実行環境の違い）

## 詰まったこと
- `setSkill(null)` でTypeScriptエラー → `useState` の型に `| null` を追加して解決
- `<Link>` でカード全体を包むと `stopPropagation` でも遷移が止まらなかった → `<Link>` をテキスト部分だけに移動して解決

## 次にやること
- Phase 2：TypeScript入門
