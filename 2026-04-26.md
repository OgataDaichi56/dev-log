# 2026-04-26 学習メモ

## 学んだトピック
- skill-tree-practice で SkillCard コンポーネントを作成
- Props の型定義（type Props）と分割代入での受け取り方
- `onClick={onToggle}` と `onClick={() => onToggle}` の違い（後者は関数を呼び出さない）
- `key` は親側（page.tsx）でつけるもの

## 詰まったこと
- 特になし

## 次にやること
- skill-tree-practice の続き（page.tsx に SkillCard を組み込む・State Lifting 確認）
- Phase 2：TypeScript入門
